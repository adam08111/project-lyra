# HANDOFF.md — Lyra session seed (for the next planning session)

> **Purpose.** This file onboards a fresh planning session — whichever model — to continue Lyra's build. It is deliberately written for *any* capable planner, not to resurrect a specific one: the continuity of this project lives in its **documents and its § log**, not in any model's memory. Every phase of this build was executed by an instance with no memory of the planning conversation, working purely from a self-contained brief. That is the pattern. It works. This file makes it work again on the first read.
>
> **This file should live in the repo** (version-controlled, evolves with the build) and be updated at the end of any session that changes the picture. It is the always-current front door; the § log is the full history.

---

## 1. What Lyra is

A mobile-first (430px) **React 18 + Vite** AI writing coach for **14-year-old Hong Kong DSE English learners**. Socratic philosophy — *"we won't write it for you"*; a fix illustrates the student's own meaning, never upgrades it. AI calls: `callAI()` → `/api/gemini` (proxy hides the key) → **Google Gemini**, three-tier (PRO / FLASH / LITE) per `ai-router.js`. Persistence is **local-first**: `localStorage` is authoritative, with an optional flag-gated Supabase mirror for durability + queryability. Tests: `npx vitest run`.

The maintainer (Adam) is a solo developer working exclusively through Claude Code, on a linear §-numbered progress log with structured task briefs. He has a quant-finance and BaZi background; he engages at an expert level and expects to be pushed back on, not flattered.

## 2. The working method (this is the load-bearing part — read it)

**The planner plans; it does not code.** The workflow is: planner reads source → writes a self-contained **task brief** (a `.md` file) → Adam hands it to a **fresh Claude Code session** that executes it → Claude Code lands the work and reports → planner reviews the report against the log. One brief = one Claude Code session = one § entry = one fast-forward landing on `origin/main`.

**The brief format** (keep it): *Context → Decisions-do-not-relitigate → New-decisions-this-plan-sets (ratify before executing) → Steps → Manual verification → Out of scope → Karpathy close.* Briefs must be self-contained (a cold executor with repo access can run them), and must open with a **Step 0** that re-reads the touched files and the § tip, with **stop-and-report** triggers if reality differs from the brief's assumptions. The planner's source snapshot is always potentially stale — **the repo wins, always.**

**The disciplines that made a fast pace safe rather than reckless** (all encoded in `CLAUDE.md` — read it every session):
- **Karpathy discipline** — read first, smallest diff, commit per unit, no drive-by refactors.
- **Push discipline** — land on `origin/main` every session; never leave work local-only; fast-forward only, never force; **record the landed `origin/main` sha in the § entry** (this slipped four times — state it every time).
- **Single source of truth** — never write a second copy of a rule/constant; extract and import.
- **Verify before fix / check-don't-assume** — on a review, list findings and try to *disprove* each before fixing; when asked "is X done?", check the code, don't re-implement.
- **Never-stuck** — every AI call resolves to content, an honest empty state, or a visible error+retry; never an eternal spinner.
- **Pedagogy is law** — see `lyra-brain.js`; never write the student's content; patterns over instances; correction-vs-taste; English-primary with 繁中 (書面語) support.

## 3. Read these first (the repo IS the memory — in this order)

1. **`CLAUDE.md`** — the non-negotiables + session checklist. Auto-loaded; read before every task.
2. **`PROGRESS-REPORT.md`** — the linear § log (currently through **§128**). The tail is the current state; the whole thing is the history and the rationale.
3. **`CHECKPOINTS.md`** — the living pending-work board (Operator / CIP-critical-path / Claude-Code-queue / pilot-term lanes), ticked with dates + § pointers; the tactical truth for "what's not done yet."
4. **`SECURITY.md`** — the security posture (§102 surface + §103 red-team + §120 class-P apolitical) and the required process (re-run the red-team before every pilot/release; class-E always human-reviewed).
5. **`DEPLOY.md`** — deploy path, the Supabase setup, the security operational notes (`ALLOWED_ORIGIN`, the rate-limit caveat, the safety thresholds, `CIVIC_INTEGRITY`).
6. **`ai-router.js`** — authoritative for the AI/proxy layer (the three-tier model map + brain-flag rationale + the real route table).
7. **`lyra-brain.js`** / **`judgment-rules.js`** / **`report-card-brain.js`** — the pedagogy and the shared judgment constants.

## 4. Current state (one paragraph)

**The P0 platform is live-proven, the teacher surface is built + live-verified (§106–§110), the data layer is durability-hardened AND its four migrations are now applied + verified against the live database by the operator's own hands (§119), the coach is apolitical by tested design (§120/§124), student + teacher-mediated recovery is productized (§121/§123), and durability is closed on both sides — an offsite encrypted backup (§125) and a one-button take-home export (§127).** The prior record still holds — P0 two-layer platform, live crown test (§101.1), §102 hardening, §103 red-team + §105 injection fix, the §106–§110 teacher surface (SELECT-only teacher-read RLS, the read-only dashboard, the §109 session isolation), and the §111–§115 data-layer pass (identity tripwires, the `writing_snapshots` ledger, the `CASCADE→RESTRICT` auth-FK P0 fix, and the canonical `DATA-ARCHITECTURE.md`). **Since then:** §116 red-team full-output capture (the §110.1 truncation closed); §117 report-card snapshots off "the glass" (migration `0008`); §118 enrolment — class codes + the `enrol_student` RPC + the phone-onboarding overlay (migration `0009`). **§119 was THE LIVE SITTING (14 Jul):** migrations `0006→0009` applied in order and each verified live — the §112 ledger capturing, the §115 crash-test **witnessing the `23503` RESTRICT error** (+ the long-owed orphan cleaned), the §117 regen, the §118 phone enrol on a real Android — plus a live red-team re-run (30 cases, **0 non-advisory FAILs**) and Adam's **human read of the four class-E transcripts** ("E1-4 ACCEPTED" — §110.1's integrity gate cleared by a human, not a model). That sitting surfaced two findings: **F1** (the identity layer needs a secure context — a dev-test blocker only; production HTTPS is secure) and **F2** (no apolitical constraint existed — the coach *scaffolded* NSL-critical and independence essays), upgraded to a pre-pilot + pre-CIP BLOCKER. **§120 fixed F2:** one shared `apolitical-rule.js` embedded in both brains (HK national-security band, symmetric refusal, published literature still analysed), red-team **class P** live 15/15; **§124** closed the residual (the brainless Lite proofread/translate routes). **§121–§123** productized recovery: the student surface (see / use / regenerate a code — migration `0010`) and Lyra's **first teacher WRITE** (enrolment-scoped teacher-mediated regen — migration `0011`). **§125** stood up custodian #3 (a nightly `age`-encrypted `pg_dump` to offsite object storage — artifacts landed, the operator's restore drill still owed); **§127** custodian #4 (a self-contained `.html` take-home export); §126/§128 swept the doc/test debt and made the proofread refusal visible. **701 tests green**, all FF-landed; tip at §128: `804120f`. **Migrations `0006`–`0009` are applied + verified live (§119); `0010` (recovery) + `0011` (teacher-regen) are authored but NOT yet applied** — the operator's next SQL-editor sitting (CHECKPOINTS A9).

## 5. The pending-work board + the threads a cold read would miss

**The tactical truth now lives in `CHECKPOINTS.md`** — the living board of everything not-yet-done across all lanes (Operator this-week / CIP critical path / Claude-Code queue / pilot-term), ticked with dates + § pointers. Read it for the current state of play. This section keeps only the narrative a bare board doesn't carry. *(The durability / identity / permanence roadmap lives in `DATA-ARCHITECTURE.md`, §114, the canonical status-labeled doc.)*

**Where the manual checks stand — "landed" became "lived" at §119 (14 Jul):** the four migrations were applied live and every gate verified by hand — the §112 ledger capturing, the §115 `23503` crash-test + the long-owed orphan cleanup, the §117 regen, the §118 phone enrol on a real Android; the live red-team re-ran clean (0 non-advisory FAILs); Adam read the four class-E transcripts ("E1-4 ACCEPTED"). **The ONE carried sub-item:** the training-chat hydration console check (owed since §101.1 — 30 seconds, still unrun; CHECKPOINTS A1/R5). Demo hygiene before any screenshot: delete the poison-probe row (`delete from learning_events where content_key = 'poison-probe#1';`) + the localhost synthetic enrol. *(Tiny §110 characterization leftovers, non-gating: the no-classes second-teacher variant; the §108 chat-producer path (proofread path proven live); roster-count eyeballed only by implication.)*

**Operator, this week (CHECKPOINTS Lane A — nothing here is blocked on anyone):** register Vercel (GitHub sign-in + **MFA**, import repo, flag-OFF deploy, the two §102 spot-checks — A4); apply migrations `0010`+`0011` live in order + the recovery / teacher-regen manual checks (synthetic data only — A9); stand up custodian #3 — GitHub MFA + private repo, the age key, `PG_MAJOR` + the five Actions secrets, a first run, **and the restore drill** — an untested backup is theatre (A10); the successor-package commit (A7) and this HANDOFF §4/§8 refresh (A8).

**Before any real student (pilot-gate, mostly NOT coding — CHECKPOINTS Lane B/D):**
- **Outside human eyes on the data-layer diffs** — no non-model has read this code. Get a human engineer to review before minors' data is live.
- **A real-Supabase CONCURRENT-load run** — every test mocks the Supabase client, so the mock-vs-reality gap is untested under load. One profile claiming works; 40 at once is unproven. This is the single biggest unproven thing.
- **The localStorage root-cause** — a real data-loss event happened during dev (the mirror saved it); the *cause* was accepted as unknown. A children's data platform should not have an unexplained loss event. Find it.
- **Operational:** MFA on Supabase/GitHub/Vercel (the operator accounts are the real crown jewels); Supabase **Pro** before a pilot (free tier pauses on inactivity — fatal for a demo; custodian #3 (§125) now gives a backup story, restore-drill pending); raise the anonymous sign-in **30/hour/IP** limit (a class of ~40 behind one school NAT trips it — F-R6); **the PDPO erasure PROCEDURE** — §115's `RESTRICT` couples identity deletion to an explicit order (delete the `students`/`teachers` row → subtree cascades → then the auth user), so the MECHANISM exists; the documented procedure + a safe operator UX are still owed.
- **Identity is dev-grade by decision** — anonymous-per-device assumes one device ≈ one student, but schools have shared/wiped machines. §121/§123 now *productize* recovery (the student see/use/regenerate surface + teacher-mediated regen), replacing the old console incantation — but the flows are unproven live (migrations `0010`/`0011` unapplied) and fork-*prevention* UX (not just §99's D8 *surfacing* of an identity change) is still owed.

**Read-with-your-own-eyes (the loop can't do this for itself):**
- The **A/B/C red-team transcripts** (and the §120 **class-P** apolitical gate) are still judged by a **sibling Gemini model** — a real structural blind spot. §119 narrowed it for class E (Adam's human read), but not for A/B/C or class P. Before any "pedagogical integrity held / IP held / politically neutral" claim goes in the **CIP application**, Adam should read the actual transcripts with a teacher's eye. The harness now saves them in full (§116) — regenerate with a live run, then read.

## 6. The Claude-Code queue + the reserve stack (per `CHECKPOINTS.md` Lane C + `DATA-ARCHITECTURE.md` §8)

**Lane C is drained** — every queued brief through §128 has landed: recovery surface (§121), teacher-mediated regen (§123), offsite dump (§125, artifacts — restore drill owed), take-home export (§127), the doc/test sweep (§126), proofread visibility (§128); the apolitical rule + its residual (§120/§124) shipped ahead of them. **What's next in code:**
- **C5 — flag-ON via staging verification** (set `VITE_*` on a preview deploy → redeploy → one reload check) before any deployed human data; pairs with Supabase **Pro** + the anon-cap raise. Operator-gated on Vercel (A4) existing first.
- **The D-Q5 pre-classifier** — a lightweight sensitive-band topic gate over student messages + pasted X-Ray sources — is a **pre-specified fast-follow, auto-triggered only by a class-P red-team FAIL** (none pending). Not built speculatively.
- **`LYRA-decomposition-brief.md`** — the `lyra.jsx` seam extraction follows §104's recorded order (header/title → type-picker → source-text → lift-skill-state → style-lab → editor → chat → grammar-log LAST → projects-as-context); each its own small brief per the §104 coupling matrix. **Post-CIP-demo work** — a rushed refactor is worse than none.

**The TOP non-code item is the CIP write-up** — mostly transcription (the security + behavioural-safety blocks in `SECURITY.md` + the § log; §119 the live-verification story; `DATA-ARCHITECTURE.md` the identity/permanence architecture; §120/§124 the apolitical posture). **Deadline 3 August 2026** (Cyberport CIP, ≤ HK$500k, needs a HK-incorporated company — the incorporation is the longest lead, Adam's to drive — CHECKPOINTS Lane B).

**Phase B (pilot-term, parked — CHECKPOINTS Lane D):** the teacher dashboard progression view (may deliberately open `report_snapshots` to teachers), exports/charts, `deriveRule` expansion, teacher rename / un-enrolment / code-rotation UI; Identity v2 attach flow behind the PDPO consent framework (B4). Small orphan-*prevention* item: tooling tabs / setup cache-clears mint empty `students` rows (the §115 D-J3 cleanup rode the live sitting).

## 7. How to work with this planner (the failure modes we hit — guard against them)

The most valuable thing this seed carries is the loop's *own known weaknesses*, so the next session inherits the lessons, not just the state:

- **The planner will assert world-state it hasn't verified.** Four times this build, the planner stated a fact about the deployed system (grants, an extension's schema, a migration's status) that was wrong, and the executor inherited the wrong framing until a real probe broke it. **Make every brief's Step 0 verify against the actual code/DB before planning on top of it.** When the planner says "X is true," the correct reflex is "confirm it."
- **A sibling model grading its own output is a real blind spot.** The red-team's automated judge is a Gemini model scoring Gemini output — it will miss failures the two share. Automated PASS is evidence, not clearance. **Human eyes on anything that goes in a funding application or gates a pilot.**
- **"Landed" is not "lived."** Green tests + a clean build + a § entry mean the code does what the planner *thinks*. The bugs that actually bit all lived in the seam between code and reality (a real DB, a real reload boundary, a real concurrent load) — exactly where the mocks can't reach. **The manual checks are not a formality; they are where reality gets a vote.**
- **The tests are green where the code and the test share an assumption.** Most of the suite (701 at §128) is pure-logic with the Supabase client mocked — they encode the planner's model of how the world behaves. A rising green count is partly rising *self-consistency*, which is not the same as safety. **Trust the tests least at the mock boundary.**
- **Push back on the planner.** This build went fast partly because the maintainer challenged the planner's confidence and the planner conceded where it was wrong. Frictionless can mean well-designed; it can also mean no independent judgment has pushed hard enough yet. The § log's honesty — recording "the planner was wrong" in plain text — is a feature. Keep it.

## 8. What this build has NOT proven (start calibrated, not confident)

So the next session doesn't inherit summary-confidence. **§119 closed several §115-era unknowns** — the four migrations (`0006`–`0009`) are applied + verified against the live DB, the `23503` crash-test was witnessed by hand, and class E is now **human-cleared** (Adam read the four transcripts, not a model). What remains open is the harder core:
- **Concurrency is untested** (mocks only) — every test mocks the Supabase client, so the mock-vs-reality gap is unproven under load. One profile claiming works; 40 at once does not. **The single biggest unproven thing.**
- **The localStorage loss is unexplained** — a real data-loss event happened in dev (the mirror saved it); the cause was accepted as unknown. A children's data platform should not have an unexplained loss event. Still owed a root-cause.
- **No outside human has read the data layer** — before minors' data is live, a non-model engineer must review it.
- **The red-team judge is still a sibling Gemini model** for classes A/B/C **and the §120 class-P apolitical gate** — automated PASS is evidence, not clearance. §119 helped (class E human-read; the §102-F4 over-block and §105 injection checks human-watched) and the harness now saves full transcripts (§116) — but the A/B/C + class-P human read is still owed before any CIP integrity claim.
- **Identity is still dev-grade** — §121/§123 productized recovery (student self-service + teacher-mediated regen), but migrations `0010`/`0011` are **authored, not yet applied**, so those flows are proven only against a mocked client; shared-device reality + fork-*prevention* UX remain.
- **Durability has mechanisms, not yet drills** — custodian #3 (§125) is artifacts-landed but its **restore drill is unrun** (an untested backup is theatre); the PDPO **erasure procedure** is undocumented (§115's `RESTRICT` gave it a mechanism, not a procedure).

None of this means the work is bad — the architecture is sound and the discipline is real; §119 walked the whole data layer against the live database by hand and it held. It means the *confidence* is structurally overstated in ways the loop can't self-correct, and the correction has to come from outside the loop: a human review, a real concurrent-load run, a root-cause, applied migrations, human-read transcripts. The engine is built and crash-tested against single-user reality (§119); it has **not** been crash-tested against pilot reality (40 concurrent students). Engine first, crash-test before passengers, passengers are children — that is the sequence, and it is correctly Adam's to run outside this loop.

---

*Written at the close of the §95–§105 session (5 July 2026); refreshed at the close of the §106–§110 run (7 July 2026 — teacher surface built + live-verified; tip `564384a`); refreshed again at the close of the §111–§115 run (8 July 2026 — identity tripwires, the writing-snapshot ledger, the auth-cascade P0 fix (`CASCADE→RESTRICT`), and `DATA-ARCHITECTURE.md` committed + corrected; tip `85bb9bd`; migrations `0006`/`0007` await their first live application); and again at §116 (11 July 2026 — red-team full-output capture: the harness now saves full class-E transcripts, closing the §110.1 tooling gap; 596 green; FF-landed tip `f21428f`; the class-E human read + a live re-run remain operator-owed); and at §117 (11 July 2026 — report-card snapshots: the growth report archived off "the glass" into append-only `report_snapshots` (migration 0008); 611 green; FF-landed tip `403a8bb`; apply 0008 + the per-issuance manual check are operator-owed); and at §118 (11 July 2026 — enrolment: class codes + the `enrol_student` RPC + the one-minute phone-onboarding overlay (migration 0009); 628 green + clean build; FF-landed tip `ead61cf`; apply 0009 + the phone-in-hand manual check are operator-owed); and — this refresh — across the §119–§128 run (14–19 July 2026): **§119 the live sitting** (migrations `0006–0009` applied + verified live, class-E human-read, live red-team 0 non-advisory FAILs; found F1/F2), **§120/§124** the apolitical rule (shared `apolitical-rule.js` + red-team class P), **§121/§123** productized recovery (the student surface + Lyra's first teacher WRITE; migrations `0010`/`0011` authored), **§125** custodian #3 (offsite encrypted backup — restore drill operator-owed), **§127** custodian #4 (take-home export), **§126/§128** the doc/test debt sweep + proofread visibility; **701 green; FF-landed tip `804120f`; §4/§8 rewritten (this A8 refresh) rather than appended**. Migrations `0010`/`0011` await first live application (A9); the restore drill (A10), Vercel (A4), and the CIP write-up (deadline 3 Aug) are operator-owed. Update this file whenever the picture changes — it is the front door, the § log is the history.*
